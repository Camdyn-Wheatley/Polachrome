opencv-python>=4.5
numpy>=1.20,<2
pyserial
pyyaml
# pylibfreenect2 is NOT listed here — it requires libfreenect2 C++ headers
# and special build constraints (Cython<3, setuptools<75).
# It is installed separately by install_kinect.sh.
